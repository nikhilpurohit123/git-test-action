name: First Workflow
on: Workflow_dispatch
jobs:
  first-job:
    runs-on:ubuntu-latest
     step:
      -name:Print greeting
       run: echo "Hello World!"
      -name:Print goodbye
       run: echo "Done -bye!"
